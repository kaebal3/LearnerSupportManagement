# Secrets and build artifacts (must not be committed)
local.properties
local.properties.txt # Just in case you used the wrong extension

# Maven and IDE artifacts
target/
dependency-reduced-pom.xml
nbproject/private/

# Other files identified as untracked that should be ignored
.DS_Store
.jdeploy/
jdeploy-bundle/
jdeploy/
lib/
merged.pdf
